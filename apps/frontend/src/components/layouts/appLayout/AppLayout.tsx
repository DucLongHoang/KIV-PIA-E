import { AppContainer } from "../../common/AppContainer"

interface Props {

}

export const AppLayout = (props: Props) => {   

    return (
        <AppContainer>
            
        </AppContainer>
    )

}